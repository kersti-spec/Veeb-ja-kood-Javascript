# Toggle Password Visibility

Simple JavaScript project that allows the user to show or hide a password input.

## What this project does
- Toggles input type between `password` and `text`
- Uses a button click event

## What I learned
- DOM element selection
- Event listeners
- Manipulating input attributes
